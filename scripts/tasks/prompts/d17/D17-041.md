# Task D17-041: Security — Discount ceiling cannot be bypassed

## What to Build

Create `apps/api/test/security/platform/platform.discount-ceiling.security.ts` to verify that no API call or parameter combination can produce a subscription rate below 85% of the base rate ($237.15).

**Security test cases:**

```typescript
import { describe, it, expect } from 'vitest';
import { calculateEffectiveRate, getEarlyBirdRate } from '@meritum/shared/utils/pricing.utils.js';
import {
  BASE_MONTHLY_RATE,
  MINIMUM_RATE_FLOOR,
  DISCOUNT_CEILING_PERCENT,
} from '@meritum/shared/constants/platform.constants.js';

describe('Security: Discount Ceiling Enforcement', () => {

  describe('Pure function boundary tests', () => {
    it('calculateEffectiveRate(279, true, true) does not go below $237.15');
    it('calculateEffectiveRate(279, true, true) returns exactly $237.15 (15% off)');
    it('calculateEffectiveRate(279, false, false) returns exactly $279.00');
    it('calculateEffectiveRate(279, true, false) returns exactly $265.05');
    it('calculateEffectiveRate(279, false, true) returns exactly $251.10');
    it('totalDiscountPercent never exceeds 15');
  });

  describe('Manipulated input tests', () => {
    it('passing a base rate of 0 still returns a non-negative rate');
    it('passing a negative base rate returns a sane result (no negative billing)');
    it('passing NaN base rate is handled gracefully');
    it('passing Infinity base rate is handled gracefully');
  });

  describe('Stripe price verification', () => {
    it('STANDARD_MONTHLY pricing constant matches $279.00');
    it('STANDARD_ANNUAL pricing constant matches $3,181.00');
    it('CLINIC_MONTHLY pricing constant matches $251.10');
    it('CLINIC_ANNUAL pricing constant matches $2,863.00');
    it('EARLY_BIRD_MONTHLY pricing constant matches $199.00');
    it('EARLY_BIRD_ANNUAL pricing constant matches $2,388.00');
    it('no pricing constant produces a monthly effective rate below $237.15 (except early bird at $199)');
  });

  describe('Early bird isolation', () => {
    it('early bird monthly rate is $199 with zero discounts');
    it('early bird annual rate is $2,388 (199*12) with zero discounts');
    it('early bird cannot have annual discount applied');
    it('early bird cannot have clinic discount applied');
    it('getEarlyBirdRate(false).totalDiscountPercent === 0');
    it('getEarlyBirdRate(true).totalDiscountPercent === 0');
  });

  describe('API-level enforcement', () => {
    it('POST /api/v1/subscriptions/checkout with CLINIC_MONTHLY creates session at $251.10');
    it('POST /api/v1/subscriptions/checkout with CLINIC_ANNUAL creates session at $2,863.00/year');
    it('checkout session always uses the server-defined price ID, not client-provided amounts');
    it('client cannot pass a custom amount to the checkout endpoint');
    it('client cannot pass a custom price_id to the checkout endpoint');
  });

  describe('Discount stacking rules', () => {
    it('annual (5%) + clinic (10%) = 15% (additive, not compounding)');
    it('15% is the absolute maximum — no combination exceeds this');
    it('verify: 279 * 0.85 = 237.15 (floor rate)');
    it('verify: 279 * 0.90 * 0.95 = 238.305 (compounding would be wrong)');
    it('the system uses additive stacking (15%), NOT multiplicative (14.5%)');
  });
});
```

**What to verify:**
1. The `calculateEffectiveRate` function enforces the ceiling at the calculation level
2. The `SubscriptionPlanPricing` constants align with the calculated rates
3. The Stripe checkout flow uses server-defined price IDs (not client-submitted amounts)
4. Early bird rates are completely isolated from the discount framework
5. No API parameter can override the server-side price selection

## Prerequisites

This task depends on: D17-002 (pricing utils), D17-003 (unit tests should pass first)

## Tests to Write

This IS the security test task. Write all tests listed above.

## Critical Security Rules

- This test file prevents revenue leakage through discount manipulation.
- The key invariant: no monthly rate below $237.15 (except early bird at $199, which is a separate flat-rate tier).
- Early bird at $199 is intentionally below the 85% floor — it is a marketing rate, not a discount. The floor applies only to discount-derived rates.
- Stripe checkout must use server-side price IDs from environment variables — the client only submits the plan name, not the price.

## Run After Completion

```bash
pnpm --filter api vitest run test/security/platform/platform.discount-ceiling.security.ts
```

All tests must pass before outputting [TASK_COMPLETE].
