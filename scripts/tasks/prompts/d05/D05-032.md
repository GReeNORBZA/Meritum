# Task D05-032: Routes: delegate management endpoints

## What to Build

Add to `apps/api/src/domains/provider/provider.routes.ts` and handlers:

**Delegate routes (auth required, physician role):**
- GET /api/v1/providers/me/delegates → listDelegatesHandler
- POST /api/v1/providers/me/delegates/invite → inviteDelegateHandler (body: inviteDelegateSchema)
- PUT /api/v1/providers/me/delegates/:rel_id/permissions → updateDelegatePermissionsHandler (body: updateDelegatePermissionsSchema, params: delegateRelIdParamSchema)
- POST /api/v1/providers/me/delegates/:rel_id/revoke → revokeDelegateHandler (params: delegateRelIdParamSchema)

**Delegate self-service routes (auth required, delegate role):**
- GET /api/v1/delegates/me/physicians → listPhysiciansHandler
- POST /api/v1/delegates/me/switch-context/:provider_id → switchContextHandler (params: switchContextParamSchema)

**Invitation acceptance (no auth required — token-based):**
- POST /api/v1/delegates/invitations/:token/accept → acceptInvitationHandler (body: acceptInvitationSchema)

Create `apps/api/test/integration/provider/delegates.integration.ts`.

## Critical Security Rules

- Physician delegate management routes require physician role.
- Delegate self-service routes require delegate role.
- Invitation acceptance is unauthenticated — secured by the token.
- Context switching logs audit event and verifies active relationship.

## Prerequisites

This task depends on the following completed tasks: D05-025

## Required Tests (MANDATORY)

You MUST write ALL 10 of the following tests. The verification step will count `it()` / `test()` calls in the test file and fail if fewer than 10 exist. Skipping or omitting tests will cause this task to fail.

**Test file:** `test/integration/provider/`

```typescript
  it('GET /providers/me/delegates returns all delegate relationships', async () => { /* implement */ });
  it('POST /providers/me/delegates/invite creates invitation', async () => { /* implement */ });
  it('POST /providers/me/delegates/invite as delegate returns 403', async () => { /* implement */ });
  it('PUT /providers/me/delegates/:rel_id/permissions updates permissions', async () => { /* implement */ });
  it('POST /providers/me/delegates/:rel_id/revoke sets REVOKED', async () => { /* implement */ });
  it('GET /delegates/me/physicians returns linked physicians for delegate', async () => { /* implement */ });
  it('POST /delegates/me/switch-context/:id succeeds with active relationship', async () => { /* implement */ });
  it('POST /delegates/me/switch-context/:id fails without active relationship', async () => { /* implement */ });
  it('POST /delegates/invitations/:token/accept activates relationship', async () => { /* implement */ });
  it('POST /delegates/invitations/:token/accept rejects expired token', async () => { /* implement */ });
```

**Total required: 10 test(s).** Do not output [TASK_COMPLETE] until all 10 tests are written and passing.

## Run After Completion

```bash
pnpm --filter api vitest run test/integration/provider/ && ./scripts/verify-tests.sh test/integration/provider/ 10
```

All tests must pass before outputting [TASK_COMPLETE].