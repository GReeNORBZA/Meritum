# Task D05-031: Routes: WCB configuration and submission preferences endpoints

## What to Build

Add to `apps/api/src/domains/provider/provider.routes.ts` and handlers:

**WCB routes (auth required):**
- GET /api/v1/providers/me/wcb → listWcbConfigsHandler
- POST /api/v1/providers/me/wcb → addWcbConfigHandler (body: createWcbConfigSchema)
- PUT /api/v1/providers/me/wcb/:id → updateWcbConfigHandler (body: updateWcbConfigSchema, params: wcbConfigIdParamSchema)
- DELETE /api/v1/providers/me/wcb/:id → removeWcbConfigHandler (params: wcbConfigIdParamSchema)
- GET /api/v1/providers/me/wcb/form-permissions → formPermissionsHandler

**Submission preferences routes (auth required):**
- GET /api/v1/providers/me/submission-preferences → getPreferencesHandler
- PUT /api/v1/providers/me/submission-preferences → updatePreferencesHandler (body: updateSubmissionPreferencesSchema)

**H-Link routes (auth required, physician only):**
- GET /api/v1/providers/me/hlink → getHlinkConfigHandler
- PUT /api/v1/providers/me/hlink → updateHlinkConfigHandler (body: updateHlinkConfigSchema)

Create `apps/api/test/integration/provider/wcb.integration.ts`, `preferences.integration.ts`, `hlink.integration.ts`.

## Critical Security Rules

- H-Link config GET response must never include credential_secret_ref.
- H-Link config PUT does not accept credential values — credential rotation is a separate secrets management operation.
- Submission preferences update requires physician role or delegate with PREFERENCE_EDIT.

## Prerequisites

This task depends on the following completed tasks: D05-024, D05-026

## Required Tests (MANDATORY)

You MUST write ALL 9 of the following tests. The verification step will count `it()` / `test()` calls in the test file and fail if fewer than 9 exist. Skipping or omitting tests will cause this task to fail.

**Test file:** `test/integration/provider/`

```typescript
  it('GET /providers/me/wcb returns all WCB configs', async () => { /* implement */ });
  it('POST /providers/me/wcb validates against WCB matrix', async () => { /* implement */ });
  it('POST /providers/me/wcb auto-populates permitted_form_types', async () => { /* implement */ });
  it('DELETE /providers/me/wcb/:id removes config', async () => { /* implement */ });
  it('GET /providers/me/wcb/form-permissions returns aggregated forms', async () => { /* implement */ });
  it('GET /providers/me/submission-preferences returns current preferences', async () => { /* implement */ });
  it('PUT /providers/me/submission-preferences updates modes', async () => { /* implement */ });
  it('GET /providers/me/hlink returns config without credentials', async () => { /* implement */ });
  it('PUT /providers/me/hlink updates submitter prefix', async () => { /* implement */ });
```

**Total required: 9 test(s).** Do not output [TASK_COMPLETE] until all 9 tests are written and passing.

## Run After Completion

```bash
pnpm --filter api vitest run test/integration/provider/ && ./scripts/verify-tests.sh test/integration/provider/ 9
```

All tests must pass before outputting [TASK_COMPLETE].