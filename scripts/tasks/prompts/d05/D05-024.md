# Task D05-024: Service: WCB configuration management

## What to Build

Add to `apps/api/src/domains/provider/provider.service.ts`:

- `addWcbConfig(providerId: string, data: CreateWcbConfigInput, actorId: string)` — validate contract_id and role_code against WCB Contract ID/Role/Form ID matrix (from Reference Data Domain 2). Auto-populate permitted_form_types from the matrix. Emit wcb_config.added audit event.
- `updateWcbConfig(providerId: string, wcbConfigId: string, data: UpdateWcbConfigInput, actorId: string)` — update skill_code or default status. Emit wcb_config.updated.
- `removeWcbConfig(providerId: string, wcbConfigId: string, actorId: string)` — delete WCB config. Verify no pending WCB claims reference this config. Emit wcb_config.removed with Contract ID and Role in audit detail.
- `listWcbConfigs(providerId: string)` — list all configs.
- `getFormPermissions(providerId: string)` — return aggregated permitted form types across all Contract IDs.
- `getWcbConfigForForm(providerId: string, formId: string)` — find the matching Contract ID/Role for a given form type. Return null if not permitted.

## FRD Reference

Domain 5 Section 2.5: WCB configurations. Multiple Contract IDs per provider.
Domain 5 Section 4.1 PRV-004: WCB setup with Contract ID dropdown and auto-populated form types.
Domain 5 Section 11.4: WCB configuration test requirements.

## Prerequisites

This task depends on the following completed tasks: D05-013

## Required Tests (MANDATORY)

You MUST write ALL 9 of the following tests. The verification step will count `it()` / `test()` calls in the test file and fail if fewer than 9 exist. Skipping or omitting tests will cause this task to fail.

**Test file:** `src/domains/provider/provider.test.ts`

```typescript
  it('addWcbConfig validates against WCB matrix', async () => { /* implement */ });
  it('addWcbConfig auto-populates permitted_form_types', async () => { /* implement */ });
  it('addWcbConfig rejects duplicate (provider, contract_id)', async () => { /* implement */ });
  it('updateWcbConfig updates skill_code', async () => { /* implement */ });
  it('removeWcbConfig deletes config scoped to provider', async () => { /* implement */ });
  it('removeWcbConfig rejects if pending claims reference config', async () => { /* implement */ });
  it('getFormPermissions returns union of all permitted forms', async () => { /* implement */ });
  it('getWcbConfigForForm returns matching config', async () => { /* implement */ });
  it('getWcbConfigForForm returns null for non-permitted form', async () => { /* implement */ });
```

**Total required: 9 test(s).** Do not output [TASK_COMPLETE] until all 9 tests are written and passing.

## Run After Completion

```bash
pnpm --filter api vitest run src/domains/provider/provider.test.ts && ./scripts/verify-tests.sh src/domains/provider/provider.test.ts 9
```

All tests must pass before outputting [TASK_COMPLETE].