# Task D05-010: Repository: provider CRUD operations

## What to Build

Create `apps/api/src/domains/provider/provider.repository.ts` with provider operations:

- `createProvider(data: InsertProvider)` — insert provider record. provider_id must match the user_id from Domain 1.
- `findProviderById(providerId: string)` — find provider by ID. Return null if not found.
- `findProviderByBillingNumber(billingNumber: string)` — find provider by billing number. Used for uniqueness validation.
- `findProviderByCpsaNumber(cpsaNumber: string)` — find by CPSA registration number.
- `updateProvider(providerId: string, data: Partial<InsertProvider>)` — update provider fields. Set updated_at to now().
- `completeOnboarding(providerId: string)` — set onboarding_completed = true. Validate all required fields are populated before setting.
- `getOnboardingStatus(providerId: string)` — return which required fields are populated and which are missing.

All queries are scoped to a single provider_id — no cross-provider queries at this layer.

## Critical Security Rules

- All queries must be parameterised — no string interpolation for provider_id or billing_number.
- findProviderById returns null if not found — callers must check and return 404.

## Prerequisites

This task depends on the following completed tasks: D05-009

## Required Tests (MANDATORY)

You MUST write ALL 9 of the following tests. The verification step will count `it()` / `test()` calls in the test file and fail if fewer than 9 exist. Skipping or omitting tests will cause this task to fail.

**Test file:** `src/domains/provider/provider.test.ts`

```typescript
  it('createProvider inserts provider record', async () => { /* implement */ });
  it('createProvider rejects duplicate billing_number', async () => { /* implement */ });
  it('createProvider rejects duplicate cpsa_registration_number', async () => { /* implement */ });
  it('findProviderById returns provider for valid ID', async () => { /* implement */ });
  it('findProviderById returns null for unknown ID', async () => { /* implement */ });
  it('updateProvider updates fields and updated_at', async () => { /* implement */ });
  it('completeOnboarding sets onboarding_completed true', async () => { /* implement */ });
  it('completeOnboarding fails if required fields missing', async () => { /* implement */ });
  it('getOnboardingStatus reports missing fields accurately', async () => { /* implement */ });
```

**Total required: 9 test(s).** Do not output [TASK_COMPLETE] until all 9 tests are written and passing.

## Run After Completion

```bash
pnpm --filter api vitest run src/domains/provider/provider.test.ts && ./scripts/verify-tests.sh src/domains/provider/provider.test.ts 9
```

All tests must pass before outputting [TASK_COMPLETE].